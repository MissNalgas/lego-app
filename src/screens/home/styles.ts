import { StyleSheet } from "react-native";

export default StyleSheet.create({
    cart: {
        width: '100%',
        padding: 16,
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center'
    }
});